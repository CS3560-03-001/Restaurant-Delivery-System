spring:
  datasource:
    url: jdbc:h2:mem:restaurant_delivery;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  frontend-origin: http://localhost:5173
